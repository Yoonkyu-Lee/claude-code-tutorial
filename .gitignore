node_modules/
.env
.env.*
*.log
.DS_Store
Thumbs.db

# 외부 MCP 서버 소스 (별도 프로젝트로 클론한 것)
youtube-mcp/

# /collect-new 런타임 캐시 (handle -> channel_id)
.claude/collect-state

# MkDocs 빌드 출력 (사이트는 GitHub Actions가 빌드·배포)
site/

# 대용량 자료 zip (커밋 금지)
*.zip

# conference 원본 자료 (대용량·저작권 웨비나 자료, 커밋 금지)
conference/materials/
